// If this file is a custom hook, it should be exported
export const useDebounce = (value: string, delay: number) => {
    // Your debounce hook logic here
    return value;
  };

  export {}; // This makes the file a module
